{
  "name": "@visual-ai/extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome Extension (Manifest V3) for browser activity monitoring and visual context capture",
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "dev": "npx tsx scripts/build.ts && echo '📦 Load dist/ as unpacked extension in Chrome'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@visual-ai/shared-types": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "concurrently": "^9.1.2",
    "esbuild": "^0.25.4",
    "typescript": "^5.5.0"
  }
}
