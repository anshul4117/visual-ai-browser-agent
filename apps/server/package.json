{
  "name": "@visual-ai/server",
  "version": "1.0.0",
  "private": true,
  "description": "Express.js backend API for browser activity event ingestion and storage",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "echo 'Dev server will be configured in Phase 3'",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@visual-ai/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
