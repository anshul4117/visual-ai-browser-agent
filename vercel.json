{
  "version": 2,
  "buildCommand": "npm run build --workspace=packages/shared-types && npm run build --workspace=apps/dashboard",
  "outputDirectory": "apps/dashboard/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
