# Server
PORT=3000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/visual-ai-browser-agent

# Extension (no secrets — configured via chrome.storage)
# API_BASE_URL=http://localhost:3000
